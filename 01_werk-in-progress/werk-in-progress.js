function setup(){
    createCanvas(windowWidth, windowHeight);
}

function draw(){
    background(0);
   
    textFont("Muli");
    fill(255);
    textSize(70);
    textAlign(CENTER, CENTER);
    text("PROG(WERK)RESS", width/2, height/2);
}